# Database
psycopg2-binary~=2.9
python-dotenv~=1.0
# SEC EDGAR
edgartools~=4.0
PyGithub~=2.0
arxiv~=1.4
# News / HTTP
requests~=2.31
feedparser~=6.0
groq~=0.4
# Observability — Langfuse tracing
langfuse>=3.0,<4.0
# AWS — Bedrock agents
boto3~=1.28
neo4j~=5.0
pandas~=2.0
# LangChain / LangGraph. Upper bounds matter here: CI reinstalls from this file
# on every push, so an unbounded major bump would break the build with no code
# change on our side. The agent factory we use lives in the 1.x line.
langgraph>=1.2,<2.0
langchain>=1.3,<2.0
langchain-aws>=1.6,<2.0
langchain-core>=1.4,<2.0
# API
fastapi~=0.104.0
uvicorn[standard]~=0.24.0
